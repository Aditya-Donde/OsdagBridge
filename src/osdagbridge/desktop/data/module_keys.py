"""
Module display keys for OsdagBridge.

These are the identifiers stored in the recents database and passed around as
``module_key``. They double as the card / tab titles, mirroring the way Osdag
uses its ``KEY_DISP_*`` constants.
"""

KEY_DISP_PLATE_GIRDER_BRIDGE = "Composite Plate Girder Bridge"
